{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build main",
            "type": "shell",
            "command": "/usr/bin/gcc main.c ./*.c -o output.o && ./output.o",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}